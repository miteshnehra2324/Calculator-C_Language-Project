#include<stdio.h>
main()
{    
    int a,b,sum;
	printf("enter the value of a and b");
	scanf("%d%d",&a,&b);
	printf("\n sum of 2 no is %d",a+b);
	printf("\n sub of 2 no is %d",a-b);
	printf("\n mult of 2 no is %d",a*b);
}
